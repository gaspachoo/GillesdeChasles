info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/login"
  body:
    type: json
    data: ""
  auth:
    type: basic
    username: admin
    password: admin1234

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
