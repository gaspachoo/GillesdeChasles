info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/login"
  body:
    type: json
    data: ""
  auth:
    type: basic

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
