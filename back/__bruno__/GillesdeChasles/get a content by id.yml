info:
  name: get a content by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/content/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
