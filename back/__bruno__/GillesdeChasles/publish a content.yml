info:
  name: publish a content
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/content"
  body:
    type: json
    data: |-
      {
        "title" : "Essai numéro 5",
        "type" : "poeme"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
